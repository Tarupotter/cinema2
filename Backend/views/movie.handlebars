

<article class="movie">
  <h1>{{ movie.title }}</h1>
  {{#if movie.image }}
  <img class="movie-page-image" src="{{ movie.image.url }}">
  {{/if }}
  <div class="intro">
    {{{ movie.intro }}}
  </div>
</article>
